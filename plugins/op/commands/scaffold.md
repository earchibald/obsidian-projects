---
description: "Scaffold a new Obsidian Projects project (folder, base, STATUS.md). Args: <slug> <PREFIX> [title] — e.g. jira-bases JB Jira-style bases."
---

Invoke the `op` skill and run the **scaffold** verb with arguments: `$ARGUMENTS`.

Follow the skill exactly. Confirm with the user if the slug is invalid or the project folder already exists.
